Installation
============

To install the latest version of ``linea`` via pip, type::

    pip install linea

To build ``linea`` from source, clone the git repository and install with::

    git clone https://github.com/bmorris3/linea.git
    cd linea
    python setup.py install
